server:
  port: 8080
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/FalconMES

  application:
    name: FalconMES

  datasource:
    url: jdbc:postgresql://localhost:5432/FalconMES
    username: postgres
    password: P@ssw0rd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

app:
  queue:
    name: demoQueue
  exchange:
    name: demoExchange
  routing:
    key: demoRoutingKey

#  data:
#    mongodb:
#      uri: mongodb://admin:admin123@localhost:27017/FalconMES?authSource=admin
#      host: localhost
#      port: 27017
#      database: FalconMES
#      username: admin
#      password: admin123
#      authentication-database: admin


